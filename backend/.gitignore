__pycache__/
*.pyc
*.db
*.log
.venv/
env/
.venv/
.env
.notes.txt
.vscode/
coverage/
.pytest_cache/

🖥️ Phase 8: Frontend Dashboard (GitHub Pages Deployment)

Goals:
	• Build a lightweight public dashboard and control panel
	• Visualize live GitHub activity and system performance
	• Provide controls for toggling ingestion and triggering pruning

Frontend Stack:
	• Vite + React + TypeScript
	• Tailwind CSS + Headless UI
	• Axios for HTTP requests
	• Recharts or Chart.js for visualizations
	• Deployed on GitHub Pages

Frontend Views to Build:
1. Dashboard View
	• Live log feed (polls /logs/live-feed)
	• Multi-line event type chart (PushEvent, CreateEvent, etc.)
	• Latency over time graph
	• Error rate visualization
	• KPI cards: total logs, error %, etc.

2. Control Panel View
	• Toggle GitHub Ingestor ON/OFF (POST /control/ingestor)
	• Manual prune trigger (POST /control/prune)
	• Optional: show task running status if API supports it

Backend Checklist:
	• [ ] Implement /logs/live-feed (returns last N log events)
	• [x] Implement /control/ingestor (toggle ON/OFF)
	• [x] Implement /control/prune (manual prune trigger)

Integration Tasks:
	• [ ] Enable CORS in FastAPI for frontend access
	• [ ] Add GitHub Pages base URL to frontend .env
	• [ ] Confirm Render is exposing API endpoints correctly

Deployment:
	• Build frontend with Vite
	• Push to GitHub repo
	• Deploy via GitHub Pages